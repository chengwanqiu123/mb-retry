# mb-retry：MoonBit 重试、熔断与限流工具库 — 项目申报书

## 1. 项目名称

**mb-retry** — MoonBit 重试、熔断与限流工具库

## 2. 项目定位

专为 MoonBit 云原生应用打造的轻量级容错工具库，提供分布式系统经典的稳定性模式：**重试策略**、**熔断器**和**限流算法**。三大能力可单独使用，也可组合成统一的弹性管道，直接包装官方 `async/http` client，为 MoonBit 后端服务提供开箱即用的服务治理能力。

> **当前状态**：核心功能已全部实现并可编译运行，包含 16 个单元测试（全部通过）及可直接运行的示例程序。

## 3. 分阶段开发规划

### 阶段一（初步优先实现，可运行版本）
1. 重试策略：固定间隔、指数退避、带抖动指数退避
2. 熔断器：Closed/Open/HalfOpen 三态状态机，可配置失败阈值与恢复窗口
3. 限流算法：令牌桶、漏桶、滑动窗口计数
4. 弹性管道：组合限流→熔断→重试，统一接口

### 阶段二（迭代完善功能）
1. 重试结果的指标统计（成功率、平均延迟、重试次数分布）
2. 熔断器的指标导出与事件回调
3. 限流的分布式令牌桶扩展接口
4. 批量请求限流（allow_n）

### 后续扩展方向
预留扩展接口，支持与 mb-timeseries 等监控库对接，输出稳定性指标到监控系统；支持异步 HTTP client 的中间件包装；支持配置文件加载。

## 4. 核心功能清单

| 模块 | 功能描述 |
| ---- | ---- |
| 重试策略 | 固定间隔 / 指数退避 / 带抖动退避，最大次数可配，支持自定义 sleep 回调 |
| 熔断器 | 经典三态状态机，失败阈值/恢复超时/半开请求数可配，自动状态转换 |
| 令牌桶限流 | 固定容量 + 恒定速率补充，支持突发流量，可一次消耗多个令牌 |
| 漏桶限流 | 请求入桶 + 恒定速率流出，平滑输出流量，防止下游过载 |
| 滑动窗口限流 | 精确时间窗口计数，自动淘汰过期请求，支持任意窗口大小 |
| 弹性管道 | 组合限流→熔断→重试，统一时间管理，一键配置，可直接包装 HTTP client |

## 5. 生态价值与目标用户

- **目标用户**：使用 MoonBit 开发后端服务、Serverless 函数、边缘计算应用的开发者；需要服务治理能力的微服务架构师。
- **生态价值**：填补 MoonBit 云原生服务治理空白。MoonBit 官方已有 `async/http` 库，但缺少重试、熔断、限流等服务稳定性组件。mb-retry 可直接包装 http client，是所有 MoonBit 后端服务的刚需基础设施。Wasm 体积小、性能高，特别适合 Serverless 冷启动和边缘计算的资源受限环境。
- **实施边界**：仅实现容错算法与弹性管道的核心逻辑，不实现网络通信、服务发现、配置中心等外部系统对接；时间管理采用外部注入模式，不绑定特定运行时的时钟 API。

## 6. 项目完整度保障方案

1. 全部核心模块提供 MoonBit 原生单元测试，覆盖边界条件、状态转换、时间依赖等场景；
2. 提供可直接运行的示例代码，演示三大能力单独使用和组合使用；
3. 编写清晰的接口文档，所有公开 API 注释完整，包含使用示例；
4. 遵循 MoonBit 社区编码规范，模块划分清晰，纯计算无外部依赖，易于后续社区贡献迭代；
5. 时间相关组件全部支持外部时钟注入，确保可测试性。

## 7. 仓库地址

本项目基于 GitHub 托管，标准 MoonBit 工程结构，包含源码、测试、示例、文档全内容，可直接通过 MoonBit 工具链编译构建。

> 仓库链接：https://github.com/chengwanqiu123/mb-retry
