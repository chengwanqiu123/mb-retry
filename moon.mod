// mb-retry: MoonBit retry, circuit breaker and rate limiting toolkit
// Learn more about moon.mod configuration:
// https://docs.moonbitlang.com/en/latest/toolchain/moon/module.html

name = "chengwanqiu123/mb_retry"

version = "0.1.0"

readme = "README.md"

repository = "https://github.com/chengwanqiu123/mb-retry"

license = "MIT"

keywords = ["retry", "circuit-breaker", "rate-limit", "resilience", "cloud-native", "moonbit"]

preferred_target = "wasm"

description = "A cloud-native resilience toolkit for MoonBit, providing retry strategies, circuit breaker pattern, and rate limiting algorithms (token bucket / leaky bucket / sliding window)."
